swop:
  base-url: https://swop.cx
  api-key: ${SWOP_API_KEY}
  timeout: 1000ms

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never

server:
  port: 8080
